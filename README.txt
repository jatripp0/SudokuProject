The template file for this game is stored in the assets folder. The file SuDokuTemplate.txt 
which is stored in the main folder for this project will be overwritten at runtime every time
the program is run.

In order to restart the program with a fresh template, copy the version in the assets folder
without changing the name into the main folder.